import { CanUserCreateItemGuard } from './can-user-create-item.guard';

describe('CanUserCreateItemGuard', () => {
  it('should be defined', () => {
    expect(new CanUserCreateItemGuard()).toBeDefined();
  });
});
